vim 
+ pathogen 
+ vimenv script forked from git://github.com/xuxiaodong/vimenv.git

Installation:

	git clone git://github.com/steve13814/dotvim.git ~/.vim

Create symlinks:

	ln -s ~/.vim/vimrc ~/.vimrc
	cd ~/.vim
	./vimenv -u	

